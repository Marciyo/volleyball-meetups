services:
  - name: volleyball-meetups-api
    type: web
    runtime: node
    region: singapore
    buildCommand: npm install
    startCommand: npm run start
    autoDeploy: true
    envVars:
      - key: PORT
        value: 3000